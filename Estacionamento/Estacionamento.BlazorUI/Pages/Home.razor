@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Sempre que abrir, vai pro login
        Nav.NavigateTo("/login", forceLoad: true);
    }
}
